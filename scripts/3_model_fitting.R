# Model Fitting (INLA/BYM2)
# Install packages as needed:
# install.packages(c('INLA','sf','spdep','INLAtools'))
library(INLA)

# Example placeholder (replace with your model code)
# result <- inla(outcome ~ predictors,
#                data = df_clean,
#                family = 'nbinomial')
# saveRDS(result, 'outputs/model_result.rds')
