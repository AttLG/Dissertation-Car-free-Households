# Data Cleaning
# Place raw data files in the 'data/' folder (git-ignored).
# Load packages
library(tidyverse)

# Example: read and clean (replace with your real code)
# df <- read_csv('data/raw_data.csv')
# df_clean <- df |> filter(!is.na(outcome))

# Save intermediate objects (optional; keep out of git if big)
# saveRDS(df_clean, 'data/cleaned.rds')
